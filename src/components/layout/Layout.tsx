import Header from "./Header";
import MainComponents from "../MainComponents";

function Layout() {
    return (
        <div>
            <Header />
            <MainComponents />
        </div>
    );
}

export default Layout;
